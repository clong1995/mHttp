class Module {
    DOM() {
        // this.addContentDom = coo.query('.addContent', DOMAIN);
    }

    EVENT() {
        //cp.on('.inner', DOMAIN, 'click', t => this.edit(t));
    }

    INIT() {

    }

    show() {
        cp.show(DOMAIN);
    }

}